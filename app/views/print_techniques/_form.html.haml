= form_for @print_technique do |f|
  - if @print_technique.errors.any?
    #error_explanation
      %h2= "#{pluralize(@print_technique.errors.count, "error")} prohibited this print_technique from being saved:"
      %ul
        - @print_technique.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
