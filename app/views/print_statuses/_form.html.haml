= form_for @print_status do |f|
  - if @print_status.errors.any?
    #error_explanation
      %h2= "#{pluralize(@print_status.errors.count, "error")} prohibited this print_status from being saved:"
      %ul
        - @print_status.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
