= form_for @transaction_type do |f|
  - if @transaction_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@transaction_type.errors.count, "error")} prohibited this transaction_type from being saved:"
      %ul
        - @transaction_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
